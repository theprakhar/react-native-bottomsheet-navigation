import SheetWrapper from "./views/sheet-wrapper";
import { pushSheet, closeAll, navigateToSheet } from "./utils/navigation";

export const BottomsheetNavigation = {
  pushSheet,
  navigateToSheet,
  closeAll,
};
export const BottomsheetWrapper = SheetWrapper;
